# Edit HTML Report V4.3 Implementation Plan

1. Add characterization tests for the frozen post-artifact editor/finalize contract and failing V4.3 tests for source-closed editorial validation, strategy diversity, atomic promotion, and chart hit testing.
2. Add an editorial-model import/validation contract and stop public variant creation from scaffolding a mode-shaped final report.
3. Upgrade Huashu input and candidate schema to complete strategy packages; bind composition and design hashes and promote them atomically.
4. Replace the fixed presentation mapper with declarative component-tree/layout/chart bindings from the confirmed strategy.
5. Add semantic chart specifications and coordinate-level line/bar/scatter interaction.
6. Upgrade CLI, schemas, package metadata, SKILL.md, references, migration notes, and agent metadata to V4.3.
7. Run unit, E2E, syntax, skill, and package validation.
8. Run the full AI server DOCX workflow, create three Huashu strategies, select the first as explicitly authorized, render and validate, then open the visible editor and stop at the unchanged human confirmation gate.


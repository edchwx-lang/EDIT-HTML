interface:
  display_name: "Edit HTML Report V5.3.0"
  short_description: "Extract facts, let Huashu own content and design, then audit and edit"
  default_prompt: "Use $edit-html-report V5.3.0 to extract a Source Pack, ask the two required content questions plus at most one material-driven clarification, store decisionEvidence, let Huashu own content and design, prepare one 1440x900 screenshot for each compact executable sample, wait for a user selection receipt before confirming the candidate, freeze the complete chosen site with an immutable stage receipt, preserve it through audit and instrumentation, complete desktop/mobile validation, and return a clickable visible-editor handoff for direct editing, in-place palette changes, version saving, and saved-version publishing with local publish, domain publish, reveal local publication, and delete-version actions."
